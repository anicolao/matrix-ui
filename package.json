{
  "name": "matrix-ui",
  "version": "0.1.0",
  "description": "A context-aware Matrix chat client",
  "type": "module",
  "scripts": {
    "dev": "echo 'Starting development environment...' && npm run dev:frontend",
    "dev:frontend": "vite dev",
    "build": "vite build",
    "ci": "echo 'Running linting and formatting checks'",
    "ci:fix": "echo 'Applying linting and formatting fixes'",
    "test": "echo 'Running all tests' && cargo test",
    "test:watch": "echo 'Running tests in watch mode'"
  },
  "devDependencies": {
    "typescript": "^5.3.3",
    "svelte": "^4.2.8",
    "@sveltejs/vite-plugin-svelte": "^3.0.1",
    "vite": "^5.0.10"
  },
  "dependencies": {
    "svelte": "^4.2.8"
  }
}